name: Upload Coverage to Coveralls

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  coveralls:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "16"

      - name: Install dependencies
        run: npm install

      - name: Run tests and generate coverage
        run: npm run test -- --coverage

      - name: Upload coverage to Coveralls
        run: npx coveralls < ./coverage/lcov.info
        env:
          COVERALLS_REPO_TOKEN: ${{ secrets.COVERALLS_REPO_TOKEN }} # Add your Coveralls token to GitHub Secrets
